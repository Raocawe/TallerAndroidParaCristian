<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cristiancastro.tallerandroid">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Maininicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".partidaActivity" />
        <activity android:name=".registroActivity" />
        <activity android:name=".inicio" />
        <activity android:name=".mejorespuntajesusuario" />
        <activity android:name=".mejorespuntajescelular" />
        <activity android:name=".loginadmin" />
        <activity android:name=".agregarpalabra" />
        <activity android:name=".modificarpalabra"></activity>
    </application>

</manifest>